precision mediump float;
varying vec4 vNormal;
varying vec3 vColor;
varying vec2 vTexCoord;
uniform sampler2D uTex;

void main()
{
   gl_FragColor = texture2D(uTex, vTexCoord) * vec4(vColor, 1.0);
}

